const express = require ("express");
const burgerjs = require ("../models/burger.js")


const router = {
    // CREATE a ROUTER to be exported

    // VIEW ROUTES

    // API ROUTES
}

module.exports = router;