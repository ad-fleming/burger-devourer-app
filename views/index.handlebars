
<div class="container my-3">
  <div class="container d-flex justify-content-center">
    <img id="burger-img" src="burger.png" alt="">
  </div>
  <h1 class="text-center">Burger Devourer</h1>
  <div class="col-sm-12 my-3">
    <div class="row d-flex justify-content-between">
      <ul class="col-sm-5">
        {{#each burgers}}
          {{#unless devoured}}
            {{> burgers/burger-block devoured=true}}
          {{/unless}}
        {{/each}}
      </ul>

      <ul class="col-sm-5">
        {{#each burgers}}
          {{#if devoured}}
            <li class="post-devoured-li d-flex my-3 justify-content-between">
              {{id}}. {{burger_name}}
            </li>
          {{/if}}
        {{/each}}
      </ul>

    </div>
  </div>
  <div class="container col-12 d-flex justify-content-center">
    <div class="row">
      <form class=" col-sm-12 create-form">
        <div class="form-group">
          <label for="burg"></label>
          <input type="text" id="burg" name="name" placeholder="ENTER A BURGER HERE!">
        </div>
        <p class="text-center">
          <button class="submit-button justify-content-center" type="submit">Add Burger</button>
        </p>
      </form>
    </div>
  </div>
  
</div>

