<li class="pre-devoured-li my-4 d-flex justify-content-between">
	{{id}}. {{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}Devour{{/if}}
	</button>

</li>
